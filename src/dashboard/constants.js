// Dashboard always defaults to today (compared to the prior day) regardless of
// the WooCommerce site-wide default date range.
export const DASHBOARD_DEFAULT_DATE_RANGE = 'period=today&compare=previous_period';
